---
# yaml-language-server: $schema=https://gitlab.com/gitlab-org/gitlab/-/raw/master/app/assets/javascripts/editor/schema/ci.json
stages:
- labels
- lint
- test
- renovate

variables:
  CI_MISE_IMAGE: ghcr.io/jdx/mise:2026.6@sha256:b319ba78c02fd28a593d043dd09d62685191420f327bf1534bdaa128f896bc93

.mise:
  variables:
    MISE_DATA_DIR: $CI_PROJECT_DIR/.cache/mise
    UV_LINK_MODE: copy
  image:
    name: $CI_MISE_IMAGE
    entrypoint: [""]
  cache:
    key:
      files:
      - .mise/mise.lock
    paths:
    - .cache/mise/
  before_script:
  - mise install
  - eval "$(mise activate bash --shims)"

include:
- .gitlab/ci/labels.yaml
- .gitlab/ci/lint.yaml
- .gitlab/ci/flate.yaml
- .gitlab/ci/renovate.yaml
