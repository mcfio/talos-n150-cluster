---
# yaml-language-server: $schema=https://gitlab.com/gitlab-org/gitlab/-/raw/master/app/assets/javascripts/editor/schema/ci.json
stages:
- labels
- lint
- test
- renovate

variables:
  CI_MISE_IMAGE: ghcr.io/jdx/mise:2026.6.9@sha256:cec0a0c10de581bece28b934704948c7103e90e761fc830ebdb6565dff46b52a

.mise:
  variables:
    MISE_DATA_DIR: $CI_PROJECT_DIR/.cache/mise
    UV_LINK_MODE: copy
  image:
    name: $CI_MISE_IMAGE
    entrypoint: [""]
  cache:
    key:
      files:
      - .mise/mise.lock
    paths:
    - .cache/mise/
  before_script:
  - mise install
  - eval "$(mise activate bash --shims)"

include:
- .gitlab/ci/labels.yaml
- .gitlab/ci/lint.yaml
- .gitlab/ci/flate.yaml
- .gitlab/ci/renovate.yaml
